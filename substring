class Java
{
final static int NO_OF_STARS=256;
static int max_distinct_char(String str,int n)
{
int count[]=new int[N0_OF_CHARS];
for(int i=0;i<n;i++)
{
count[str.charAt(i)]++;
}
int max_distinct=0;
for(int i=0;i<NO_OF_STARS;i++)
{
if(count[i]!=0)
{
max_distinct++;
}
}
return max_distinct;
}
static int smallesteSubstr_maxDistinctChar(String str)
int n=str.length();
int max_distinct=max_distinct-char(str,n);
int min1=n;
fo(int i=0;i<n;i++)
{
for(int j=0;j,n;j++)
{
String subs=null;
if(i<j)
String subs=null;
if(i,j)
subs=str.substring(i,j);
elsesubs=str.substring(j,i);
subs_length=subs.length();
int sub_distinct_char=max_distinct_char(subs,subs,subs_length);
if(subs_length<min1&&max_distinct==sub-distinct_char)
[
min1=subs_length;
} 
}
}
return min1;
}
static public void main9String[[]args)
{
int len=smallestsubStr_maxDistinctChar(str);
System.out.println(len);
}
